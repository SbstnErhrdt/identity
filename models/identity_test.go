package models

import (
	"testing"
)

func TestUser_VerifyPassword(t *testing.T) {
	/*
		pw := "Hello World"

			user := Identity{}
			user.SetNewPassword(pw)

			ass := assert.New(t)
			ass.True(user.VerifyPassword(pw))
			ass.True(user.VerifyPassword(string([]byte(pw))))
			ass.False(user.VerifyPassword("stupidWrongPassword"))

			user.SetNewPassword("pw2")

			ass.False(user.VerifyPassword(pw))
			ass.False(user.VerifyPassword(string([]byte(pw))))
			ass.False(user.VerifyPassword("stupidWrongPassword"))
			ass.True(user.VerifyPassword("pw2"))

	*/

}
